masTest {
	mas = "blocksworld.mas2g".
	timeout = 10.
	
	stackbuilder {
		test {		
			evaluate {
				always do(move(X,Y)) -> eventually bel(on(X,Y)).
				always not(do(move(X,X))).
			} in do main.
		}
	}
}