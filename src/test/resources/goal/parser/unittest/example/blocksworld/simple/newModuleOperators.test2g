masTest {
	mas = "blocksworld.mas2g".
	timeout = 10.
	
	stackbuilder {
		test {		
			evaluate {
				atstart[init] bel(testBelief).
				atend[init] bel(testInsert).
				atstart[main] bel(on(_,'table')).
				atstart[build] bel(block('b1')).
				atend[build] bel(on('b1','table')).
				atend[event] bel(on(X,Y)).
			} in do main.
		}
	}
}