%Correct: minimal version of evaluate in statement.
%New: variables in an evaluate ... in block should keep their scope -Vincent

masTest {
	mas = "correctMasUnderTest.mas2g".

	agentUnderTest {
		test {
			do insert(test(123)).
			evaluate {
				always bel(X = test1).
				never bel(X = test2).
				
				always bel(test(Y)).
				always bel(Y = 123).
				never bel(Y = 321).
			} in do moduleUnderTest(identifier, 42, 0.42, function).
		}		
	}
}